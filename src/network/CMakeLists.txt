add_library(eBrowser_network STATIC http.c url.c cache.c cookies.c)
target_include_directories(eBrowser_network PUBLIC ${CMAKE_SOURCE_DIR}/include)
if(WIN32)
    target_link_libraries(eBrowser_network PRIVATE ws2_32)
endif()
